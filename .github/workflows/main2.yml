on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: "before"
        run: ls

      - uses: actions/checkout@v3
        run: echo sharif

      - name: " after"
        run: ls
